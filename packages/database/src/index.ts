export { db, type DbClient } from './client.js';
export { PrismaClient, Prisma } from '@prisma/client';
// Domain types will be re-exported here as Phase 1 schema is added:
// export type { GuildConfig, Panel, PanelTicketType, Ticket, TicketEvent, TicketStatus } from '@prisma/client';
